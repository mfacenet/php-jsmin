// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("jsmin", "for jsmin support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("jsmin", "enable jsmin support", "no");

if (PHP_JSMIN != "no") {
	EXTENSION("jsmin", "jsmin.c");
}

